meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 20

build:
  # Если Amvera ругается на 'commands', попробуй написать 'command' (в единственном числе)
  command: npm install && npx prisma generate && npm run build

run:
  command: npm start