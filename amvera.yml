meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 20

build:
  command: npm install && npx prisma generate && npx prisma db push --accept-data-loss && npm run build

run:
  command: npm start